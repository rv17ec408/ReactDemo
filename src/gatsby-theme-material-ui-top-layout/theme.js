import { createTheme } from "@mui/material";
export const theme = createTheme({
  palette: {
    mode: "light",
    // primary: {
    //   main: "#f44336",
    // },
  },
  typography: {
    line: {
      fontSize: 18,
      fontStyle: "italic",
    },
  },
});
